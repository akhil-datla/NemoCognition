// Placeholder for future recovery automation. Today, recovery branches are
// created interactively via POST /api/runs/:runId/fix-and-rerun; this file is
// reserved for batch reruns triggered from the worker (e.g. retry after a
// transient policy misconfiguration is patched).
export {};
